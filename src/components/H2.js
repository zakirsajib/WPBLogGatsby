import styled from 'styled-components';

const H2 = styled.h2`
  color: #3e465b;
  font-size: calc(25px + (35 - 25) * ((100vw - 320px) / (1920 - 320)));
  font-weight: 700;
  line-height: 1.15;
  margin-bottom:10px;
  text-transform:none;
  
`;

export default H2;